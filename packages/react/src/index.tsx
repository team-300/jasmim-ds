import { colors } from '@jasmim-ds/tokens'

export type ButtonProps = {
  size: 'sm'
}

export const Button = () => {
  return <button style={{ color: colors['brand-dark'] }}>Lucas teste</button>
}
